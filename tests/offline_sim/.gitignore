# qmake build 产物(独立工程,与项目根 .gitignore 互补)
test_log_parser
sanity_check
calibrate_rpm_to_speed
offline_sim
target_wrapper.sh
*.app/

# 标定运行结果(测试机上跑 calibrate 后产生,数据特定不入库)
calib_run_*/

# 注:offline_sim.ini 现在是入库的(K 是皮带机械常数,跨机器跨时间复用),
# 见 §7.3。如果某台特殊机器需要本地覆盖,通过 main.cpp 的 --k 命令行参数即可。

